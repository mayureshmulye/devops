apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx2
  namespace: default
spec:
  ingressClassName: nginx
  rules:
  - host: nginx2.mayureshmulye.publicvm.com
    http:
      paths:
      - backend:
          service:
            name: nginx2
            port:
              number: 80
        path: /
        pathType: Exact
status:
  loadBalancer:
    ingress:
    - ip: 10.0.0.57
